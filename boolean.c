#include <stdio.h>

int main()
{
    _Bool programing_is_fun = 1; // 1 is true, 0 is false
    if (programing_is_fun)
    {
        printf("Yes it is!\n");
    }
    _Bool life_is_fair = 0; // 0 is false
    if (!life_is_fair)
    {
        printf("Life is not fair, get used to it!\n");
    }
    return 0;
}